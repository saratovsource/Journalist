%header.page-header.pos-r{style: "background:#EAEEF3; border-color: #D1DCE4; box-shadow: 0 1px 0 #F5F8FC;"}
	.box
		%h2
			= t(".#{edit_type}")
			%small
				&rarr;
				%span.iconweb
					= resource.try(:state_sym)
				= t("states.#{resource.state}")
			%span.pull-right
				%span.iconweb
					N
				?
			%span.pull-right
				%span.iconweb
					c
				?