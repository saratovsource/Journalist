%header.page-header.pos-r{style: "background:#EAEEF3; border-color: #D1DCE4; box-shadow: 0 1px 0 #F5F8FC;"}
	.box
		%h2= t(".#{edit_type}")
	- item.state_paths(:from => :drafted, :to => item.state_name).to_states.each do |state|
		%span= state