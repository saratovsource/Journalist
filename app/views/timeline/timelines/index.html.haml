- content_for :sidebar do
  = render_section(:sidebar)
.span18
  = render "shared/elements/sort_widget_tab", :active_tab => params[:tab].try(:to_sym) if params[:date].blank?
  - if @items.any?
    - @items.each do |item|
      = item.render(:block)
      .bgoverlay{:title => t('.bgoverlay'), :data => {:elements => "article.active"}}
