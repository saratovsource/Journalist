.tab-pane#journal-best
  -items = current_site.journal_articles.weekly.highest_rated(3)
  - if items.any?
    - items.each do |art|
      = render :partial => "item", :locals => {:art => art}
  - else
